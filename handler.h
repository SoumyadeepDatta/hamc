#ifndef HANDLER_H
#define HANDLER_H

void hello();
_Bool is_bin_str(char* s); // Function to check if a string is a binary string
char* remove_spaces(char* str); // Function to remove spaces from a string

#endif